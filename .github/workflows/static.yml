# Legacy GitHub Pages deploy stub.
# This repository uses the build-aware workflow in deploy-pages.yml.
name: Legacy Pages deploy (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled placeholder
        run: echo "This workflow is intentionally disabled. Use deploy-pages.yml instead."
